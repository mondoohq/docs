# `isUnspecified`

Return `true` if the IP is unspecified (`0.0.0.0` for IPv4 or `::` for IPv6).

---

## Examples

Unspecified IPv4:

```coffee
ip("0.0.0.0/0").isUnspecified
```

Unspecified IPv6:

```coffee
ip("::/0").isUnspecified
```
